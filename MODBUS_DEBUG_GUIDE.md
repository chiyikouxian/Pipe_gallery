# Modbus设备调试完整指南

## 📊 当前状态概览

### ✅ 已调试完成的传感器（4/8）
- ✅ **氧气传感器** - ADC1 Channel 18, 空气中20.9%
- ✅ **甲烷传感器GM-402B** - UART2, 19200波特率
- ✅ **温湿度传感器SHT30** - I2C1
- ✅ **火焰传感器** - 5通道ADC，阈值60000

### ⏳ 待调试的Modbus设备（4/8）
- ⏳ **应力传感器GMY400** - 从站地址3，**波特率不匹配问题**
- ⏳ **位移传感器** - 从站地址2（已改）
- ⏳ **电表（三相）** - 从站地址73
- ⏳ **水流量计** - 从站地址1

---

## 🚨 关键问题识别

### 问题1：应力传感器GMY400波特率不匹配 ⚠️⚠️⚠️

**问题描述：**
```c
// 当前系统Modbus配置（freeModbusApp.h）
#define PORT_BAUDRATE   9600

// GMY400应力传感器标称参数
// 波特率: 2400 或 4800
```

**这是应力传感器无法通信的主要原因！**

**解决方案：**
1. 使用新增的 `stress_baudrate_scan` 命令自动扫描波特率
2. 确认正确波特率后，修改 `freeModbusApp.h` 中的 `PORT_BAUDRATE`
3. 重新编译烧录

### 问题2：位移传感器与水流量计地址冲突（已修复）

**原配置：**
```c
#define DISPLACEMENT_SLAVE_ADDR  1  // 旧配置，与水表冲突
#define WATERMETER_SLAVE_ADDR    1  // 冲突！
```

**当前配置：**
```c
#define DISPLACEMENT_SLAVE_ADDR  2  // ✅ 已修改为地址2
#define WATERMETER_SLAVE_ADDR    1  // 水表保持地址1
```

### 问题3：电表地址73需要大缓冲区（已确认）

**配置检查：**
```c
// packages/freemodbus-latest/modbus/include/mbconfig.h
#define MB_MASTER_TOTAL_SLAVE_NUM  80  // ✅ 已修改为80，支持地址73
```

**缓冲区大小：**
```c
// rtconfig.h
#define RT_M_REG_INPUT_NREGS      100   // ✅ 足够
#define RT_M_REG_HOLDING_NREGS    1000  // ✅ 足够
```

---

## 🛠️ 调试步骤（按优先级）

### 第一步：系统诊断（2分钟）

在串口终端执行：

```bash
# 1. 查看所有传感器状态汇总
sensors

# 2. 查看Modbus设备详细状态
modbus

# 3. 扫描Modbus总线（地址1-10）
stress_scan_bus
```

**预期结果分析：**

| 设备 | 从站地址 | 预期状态 | 如果失败 |
|------|---------|---------|---------|
| 位移传感器 | 2 | ✅ 应该有响应 | 检查接线和地址跳线 |
| 水流量计 | 1 | ✅ 可能有响应 | 检查Modbus连接 |
| 应力传感器 | 3 | ❌ 很可能无响应 | **波特率不匹配** |
| 电表 | 73 | ⚠️ 超出扫描范围 | 需要单独测试 |

---

### 第二步：应力传感器GMY400波特率扫描（重点！）

**使用新增的自动扫描命令：**

```bash
# 自动扫描所有常用波特率（2400, 4800, 9600, 19200）
stress_baudrate_scan

# 或者测试单个波特率
test_baudrate 2400 3    # 测试2400波特率，从站地址3
test_baudrate 4800 3    # 测试4800波特率，从站地址3
```

**扫描过程：**
1. ✅ 测试2400波特率（GMY400默认）
2. ✅ 测试4800波特率（GMY400备选）
3. ✅ 测试9600波特率（当前系统设置）
4. ✅ 测试19200波特率（参考）

**根据扫描结果：**

#### 情况A：找到正确波特率（例如2400）

```
✓✓✓ SUCCESS! Device responds at 2400 baud ✓✓✓

  Raw Data:
    Reg[0] = 0x1234 (4660)
    Reg[1] = 0x5678 (22136)
    Combined = 0x12345678
  Interpreted as:
    Float: 5.690e-28
    Stress (0-400kN): 72.05 kN
```

**立即行动：**

1. **修改 `freeModbusApp.h`**
   ```c
   // 将波特率从9600改为2400
   #define PORT_BAUDRATE   2400  // 修改这里
   ```

2. **重新编译**
   ```bash
   scons -c
   scons
   ```

3. **烧录并重启**

4. **验证应力传感器**
   ```bash
   test stress
   modbus
   ```

#### 情况B：所有波特率都失败

**可能原因：**
1. ❌ RS485接线错误（485A/485B反接）
2. ❌ 从站地址不是3（需要扫描）
3. ❌ 供电问题（DC18V电源）
4. ❌ 传感器硬件故障

**排查步骤：**

```bash
# 1. 扫描所有地址（假设波特率是2400）
# 需要临时修改PORT_BAUDRATE为2400后重新编译

# 2. 检查硬件接线
# 红线: DC18V正极
# 白线: GND负极
# 蓝线: 485A (信号+)
# 绿线: 485B (信号-)

# 3. 使用外部Modbus工具验证
# Windows: Modbus Poll
# Linux: mbpoll -a 3 -r 0 -c 2 -t 4:float -b 2400 /dev/ttyUSB0
```

---

### 第三步：位移传感器调试

**现状：**
- 从站地址：2（已从1改为2）
- 寄存器：0x0000, 2个寄存器
- 零点偏移：74
- 缩放因子：0.025

**测试命令：**

```bash
# 1. 查看当前位移值
test displacement

# 2. 查看Modbus状态
modbus

# 3. 如果值为0.000mm，检查：
#    - 从站地址是否正确（实际硬件是否设为2？）
#    - 寄存器地址是否正确
#    - 接线是否正常
```

**如果位移值始终为0.000：**

1. **扫描地址1和2**
   ```bash
   stress_scan_bus  # 会扫描地址1-10
   ```

2. **检查实际硬件地址**
   - 位移传感器的地址拨码开关可能还设为1
   - 需要物理修改为2，或者代码改回1

3. **验证寄存器地址**
   - 当前代码读取 `Reg[1]`（第二个寄存器）
   - 可能需要读取 `Reg[0]`（第一个寄存器）

---

### 第四步：电表调试（三相电压电流）

**配置：**
```c
#define AMMETER_SLAVE_ADDR    73    // 从站地址73
#define VOLTAGE_REG_START     0x100 // 电压寄存器起始256
#define CURRENT_REG_START     0x10E // 电流寄存器起始270
```

**测试命令：**

```bash
# 查看电表数据
test ammeter

# 查看Modbus状态
modbus
```

**预期结果：**
```
[Ammeter]
  Voltage: P0=220.00V P1=220.00V P2=220.00V
  Current: P0=1.23A P1=2.34A P2=3.45A
```

**如果所有值为0.00：**

#### 方案A：验证从站地址73是否正确

**问题：** 代码中地址定义为73，但注释中提到过地址2。

**验证步骤：**

1. **检查实际硬件地址标签**
   - 查看电表外壳上的地址标签
   - 可能是2、49、或73

2. **使用外部工具验证**
   ```bash
   # 测试地址73
   mbpoll -a 73 -r 256 -c 6 -t 4:float -b 9600 /dev/ttyUSB0
   
   # 测试地址2
   mbpoll -a 2 -r 256 -c 6 -t 4:float -b 9600 /dev/ttyUSB0
   
   # 测试地址49（十六进制0x31，可能是49）
   mbpoll -a 49 -r 256 -c 6 -t 4:float -b 9600 /dev/ttyUSB0
   ```

3. **如果发现实际地址不是73，修改代码：**
   ```c
   // freeModbusApp.h
   #define AMMETER_SLAVE_ADDR    2   // 或49，根据实际情况
   ```

#### 方案B：验证寄存器地址映射

**问题：** 电压和电流的寄存器地址可能不正确。

**根据注释中的查询命令反推：**

```c
/* Ammeter voltage query: 49 03 01 00 00 06 CB BC */
// 49 = 十进制73（从站地址）
// 03 = 读保持寄存器
// 01 00 = 寄存器起始地址 0x0100 (256) ✅ 当前代码正确
// 00 06 = 读取6个寄存器 ✅ 当前代码正确

/* Ammeter current query: 49 03 01 0E 00 06 AA 7F */
// 49 = 十进制73
// 03 = 读保持寄存器
// 01 0E = 寄存器起始地址 0x010E (270) ✅ 当前代码正确
// 00 06 = 读取6个寄存器 ✅ 当前代码正确
```

**结论：** 寄存器地址配置正确，问题很可能是从站地址不匹配。

---

### 第五步：水流量计调试

**配置：**
```c
#define WATERMETER_SLAVE_ADDR   1   // 从站地址1
#define FLOW_REG_START          1   // 寄存器起始1
#define FLOW_REG_NUM            2   // 2个寄存器（32位浮点数）
```

**测试命令：**

```bash
# 查看node[0].Flow值
sensors

# 查看Modbus状态
modbus
```

**预期结果：**
```
Flow: X.XXX m³/h
```

**如果值为0.000：**

1. **确认水表是否真的在地址1**
   ```bash
   stress_scan_bus  # 查看地址1是否响应
   ```

2. **区分水表和位移传感器**
   - 水表：地址1，寄存器1-2
   - 位移传感器：地址2，寄存器0-1
   
3. **验证水表是否有流量**
   - 实际管道中是否有水流动
   - 水表是否通电
   - 水表显示屏是否正常

---

## 📝 调试记录模板

### 应力传感器GMY400

```
日期：2026-09-18
测试人员：___________

1. 波特率扫描结果：
   [ ] 2400 baud: ___________
   [ ] 4800 baud: ___________
   [ ] 9600 baud: ___________
   [ ] 19200 baud: ___________

2. 正确波特率：_________ baud

3. 从站地址：_________

4. 应力值：_________ kN

5. 在线状态：[ ] Online  [ ] Offline

6. 备注：
   _________________________________
   _________________________________
```

### 位移传感器

```
1. 从站地址（硬件跳线）：_________
2. 从站地址（代码配置）：2
3. 位移值：_________ mm
4. 原始ADC值：Reg[0]=_____ Reg[1]=_____
5. 状态：[ ] 正常  [ ] 异常
6. 备注：_________________________________
```

### 电表

```
1. 从站地址（硬件标签）：_________
2. 从站地址（代码配置）：73
3. 电压：P0=_____V P1=_____V P2=_____V
4. 电流：P0=_____A P1=_____A P2=_____A
5. 状态：[ ] 正常  [ ] 异常
6. 备注：_________________________________
```

### 水流量计

```
1. 从站地址：1
2. 流量值：_________ m³/h
3. 原始寄存器：Reg[1]=_____ Reg[2]=_____
4. 实际水流：[ ] 有流动  [ ] 无流动
5. 状态：[ ] 正常  [ ] 异常
6. 备注：_________________________________
```

---

## 🔧 快速故障排查表

### Modbus设备完全无响应

**症状：** 所有Modbus设备都读取失败

**排查步骤：**
1. [ ] 检查UART3引脚：PB10(TX), PB11(RX)
2. [ ] 检查RS485转换器是否通电
3. [ ] 检查485A和485B接线
4. [ ] 使用 `list_thread` 检查Modbus线程是否运行
5. [ ] 检查 `md_m_poll` 和 `md_m_send` 线程状态

### 单个设备无响应

**症状：** 其他设备正常，某一个设备无响应

**排查步骤：**
1. [ ] 确认从站地址（硬件 vs 代码）
2. [ ] 确认波特率（硬件 vs 系统）
3. [ ] 确认该设备的供电
4. [ ] 使用 `stress_scan_bus` 扫描总线
5. [ ] 使用外部工具验证设备

### 数据值异常

**症状：** 设备有响应，但数据值明显错误

**排查步骤：**
1. [ ] 检查数据格式（32位浮点 vs 16位整数）
2. [ ] 检查字节序（大端 vs 小端）
3. [ ] 检查寄存器起始地址
4. [ ] 检查换算公式和单位
5. [ ] 对比外部工具读取的原始数据

---

## 📚 调试命令速查表

### 综合诊断命令

```bash
sensors               # ⭐ 显示所有传感器汇总（最常用）
modbus                # 显示Modbus设备状态
monitor 1             # 实时监控（每1秒刷新）
```

### Modbus专用命令

```bash
stress_scan_bus       # 扫描Modbus总线（地址1-10）
stress_baudrate_scan  # ⭐ 自动扫描应力传感器波特率
test_baudrate 2400 3  # 测试指定波特率和地址
stress_test_addr 3    # 测试指定地址
stress_set_addr 1 3   # 修改从站地址（1改为3）
```

### 单个传感器测试

```bash
test flame            # 火焰传感器 ✅
test sht30            # 温湿度传感器 ✅
test methane          # 甲烷传感器 ✅
test o2               # 氧气传感器 ✅
test displacement     # 位移传感器 ⏳
test stress           # 应力传感器 ⏳
test ammeter          # 电表 ⏳
```

### 环境传感器命令

```bash
env                   # 温湿度+氧气+甲烷
flame                 # 火焰传感器详情
o2_debug              # O2传感器原始ADC调试
uart2_debug 5         # 甲烷传感器UART原始数据（5秒）
```

### 系统命令

```bash
list_thread           # 列出所有线程
free                  # 查看内存使用
ps                    # 进程状态
```

---

## ✅ 验收标准

### 全部传感器调试完成的标准：

- [ ] **氧气传感器**：20.9% 左右（空气）
- [ ] **甲烷传感器**：正常环境0 ppm，检测到气体时显示实际值
- [ ] **温湿度传感器**：温度和湿度在合理范围
- [ ] **火焰传感器**：5个通道ADC值正常，无误触发
- [ ] **应力传感器**：在线，显示合理kN值（0-400kN）
- [ ] **位移传感器**：显示合理mm值
- [ ] **电表**：三相电压~220V，电流根据负载显示
- [ ] **水流量计**：显示合理流量值
- [ ] 运行 `sensors` 命令无异常提示
- [ ] 系统稳定运行，无崩溃

---

## 🔄 下一步计划

### 调试完成后的集成任务

1. **启用WiFi/MQTT上报** (main.c中已注释)
   ```c
   // huawei_cloud_init();  // 取消注释
   ```

2. **启用LoRa数据传输**
   ```c
   // 已启用 lora_thread
   ```

3. **启用以太网TCP上报**
   ```c
   // 已启用 eth_app_init()
   ```

4. **完整系统测试**
   - 所有传感器同时运行
   - 数据上报验证
   - 长时间稳定性测试

---

## 📞 技术支持

### 硬件参数

**STM32H743XI (ARM Cortex-M7)**
- UART3 (Modbus): PB10 (TX), PB11 (RX)
- I2C1 (SHT30): PB6 (SCL), PB7 (SDA)
- ADC1 Ch18 (O2): PA4
- ADC多通道 (火焰): CH0, CH1, CH3, CH4, CH5

**应力传感器GMY400**
- 量程：0-400kN
- 精度：±4% FS
- 供电：DC18V, ≤60mA
- 波特率：2400 或 4800
- 接线：红(+18V), 白(GND), 蓝(485A), 绿(485B)

### 文件位置

- Modbus配置：`applications/Apps/freeModbusApp.h`
- 应力传感器：`applications/Apps/stressSensorApp.c/h`
- 调试命令：`applications/Apps/debugCommands.c`
- 波特率测试：`applications/Apps/modbus_baudrate_test.c`
- 主程序：`applications/main.c`

---

**最后更新：2026-09-18**
**作者：ideapad15s**
**版本：v1.0**
