# 位移传感器集成说明

## 硬件连接

### CN2 接口定义（5针）
根据原理图 `Netlist_Schematic1_2026-08-31.tel`，CN2 是一个 5 针接口：

```
CN2 引脚定义（需要根据实际原理图确认）：
Pin 1: A (RS485 A+)
Pin 2: B (RS485 B-)
Pin 3: 未知
Pin 4: GND
Pin 5: +18V
```

### 位移传感器接线
根据拉绳位移传感器文档，典型接线为：
- **A+**: 接 CN2 的 A 信号线
- **B-**: 接 CN2 的 B 信号线
- **GND**: 接 CN2 的 GND
- **电源**: 接 CN2 的 +18V（或根据传感器要求）

⚠️ **重要**: 在接线前，请确认：
1. 位移传感器的供电电压（常见为12V、24V）
2. CN2 提供的电源是否匹配
3. RS485 的 A/B 极性是否正确

## Modbus 通信参数

### 默认参数（基于位移传感器文档）
- **协议**: Modbus RTU
- **波特率**: 9600
- **数据位**: 8
- **停止位**: 1
- **校验位**: None
- **从站地址**: 0x01（默认为1，可通过命令修改）
- **功能码**: 0x03（读保持寄存器）

### 寄存器地址
- **0x0000**: 当前位移值（32位浮点数，占用2个寄存器）
- **0x0100**: 从站地址设置寄存器
- **0x0101**: 波特率设置寄存器

## 软件集成

### 已添加的文件
1. `applications/Apps/displacementSensorApp.h` - 位移传感器头文件
2. `applications/Apps/displacementSensorApp.c` - 位移传感器实现

### 已修改的文件
1. `applications/heads.h` - 添加了 `#include "displacementSensorApp.h"`
2. `applications/main.c` - 添加了位移传感器测试线程
3. `applications/Apps/NodeApp.h` - 在 Node 结构体中添加了 `float Displacement` 字段

## 测试步骤

### 1. 编译项目
```bash
cd /c/Users/ideapad15s/Desktop/Pipe_gallery_node
scons
```

### 2. 烧录到板子
使用 RT-Thread Studio 或其他工具将编译好的固件烧录到 STM32H743XI 板子。

### 3. 查看串口输出
连接 UART1（控制台）查看输出信息。启动后会看到：

```
========================================
  Displacement Sensor Test Started
========================================
  Slave Address: 1
  Register Start: 0x0000
  Register Count: 2
  Baudrate: 9600
  Parity: None
========================================
```

### 4. 观察测试结果

#### 成功的输出示例：
```
[00001] Displacement: 125.456 mm | Reg[0]=0x42FA, Reg[1]=0x3A5E | OK: 1, ERR: 0
[00002] Displacement: 126.123 mm | Reg[0]=0x42FC, Reg[1]=0x1F7D | OK: 2, ERR: 0
```

#### 通信错误的输出示例：
```
[00001] Displacement Error: Code=3 | OK: 0, ERR: 1
[00002] Displacement Error: Code=3 | OK: 0, ERR: 2
```

### 5. 常见问题排查

#### 问题1：持续报错 `Error: Code=3`
**可能原因**：
- RS485 接线错误（A/B 反了）
- 传感器从站地址不是 1
- 波特率不匹配
- 传感器未上电或损坏

**排查步骤**：
1. 用万用表测量传感器电源是否正常
2. 交换 A/B 线重试
3. 检查传感器铭牌上的从站地址
4. 尝试其他波特率（在 freeModbusApp.h 中修改 `PORT_BAUDRATE`）

#### 问题2：读到的位移值为 0.000 或异常值
**可能原因**：
- 寄存器地址错误
- 数据格式不匹配（不是标准 IEEE 754 浮点数）

**排查步骤**：
1. 查看原始寄存器值（Reg[0] 和 Reg[1]）
2. 对照位移传感器手册确认寄存器地址
3. 确认数据格式（可能是整数、定点数等）

#### 问题3：与其他 Modbus 设备冲突
**当前配置**：
- 水表：从站地址 1
- 电表：从站地址 73
- 位移传感器：从站地址 1（**与水表冲突！**）

**解决方案**：
修改位移传感器的从站地址为未使用的地址（例如 2）。修改方法：
1. 查阅位移传感器手册的"从站地址设置"章节
2. 使用 Modbus 命令或配置软件修改从站地址
3. 修改 `displacementSensorApp.h` 中的 `DISPLACEMENT_SLAVE_ADDR` 定义

## 集成到主 Modbus 轮询

确认测试通过后，将位移传感器集成到主轮询线程：

### 步骤1：注释测试线程
在 `main.c` 中注释掉位移传感器测试线程的创建代码。

### 步骤2：更新 freeModbusApp.c
在 `send_thread_entry()` 函数中添加位移传感器的读取逻辑：

```c
void send_thread_entry(void *parameter)
{
    eMBMasterReqErrCode error_code = MB_MRE_NO_ERR;
    uint16_t reg_high, reg_low;

    while (1)
    {
        /* ==================== 读取水表流量 ==================== */
        // ... 现有代码 ...

        /* ==================== 读取位移传感器 ==================== */
        error_code = eMBMasterReqReadHoldingRegister(
            DISPLACEMENT_SLAVE_ADDR,
            DISPLACEMENT_REG_START,
            DISPLACEMENT_REG_NUM,
            RT_WAITING_FOREVER
        );

        if (error_code == MB_MRE_NO_ERR)
        {
            reg_high = usMRegHoldBuf[DISPLACEMENT_SLAVE_ADDR - 1][DISPLACEMENT_REG_START];
            reg_low  = usMRegHoldBuf[DISPLACEMENT_SLAVE_ADDR - 1][DISPLACEMENT_REG_START + 1];

            g_displacement_value = displacement_registers_to_float(reg_high, reg_low);
            node[0].Displacement = g_displacement_value;

            /* 可选：打印调试信息 */
            // rt_kprintf("[Displacement] Value: %.3f mm\n", g_displacement_value);
        }

        rt_thread_mdelay(500);

        /* ==================== 读取电表电压 ==================== */
        // ... 现有代码 ...
    }
}
```

### 步骤3：更新 freeModbusApp.h
添加位移传感器相关的宏定义和外部变量声明。

### 步骤4：验证集成
重新编译烧录，确认所有传感器都能正常轮询。

## 附加功能：手动查询命令

可以在 finsh shell 中添加手动查询命令，方便调试：

```c
#include <finsh.h>

static void disp_status(void)
{
    displacement_print_status();
}
MSH_CMD_EXPORT(disp_status, Print displacement sensor status);
```

使用方法：
```
msh > disp_status
[Displacement] Online - Value: 125.456 mm
```

## 数据上报

位移数据已添加到 `Node` 结构体中，会自动通过以下路径上报：
- Huawei Cloud MQTT（需在 huaweiCloudApp.c 中添加字段）
- LoRa 透传（需在 loraApp.c 中添加字段）
- Ethernet TCP JSON（需在 ethApp.c 中添加字段）

## 注意事项

1. **从站地址冲突**：当前水表使用地址 1，位移传感器也使用地址 1，需要修改其中一个设备的地址。
2. **轮询间隔**：当前每个设备轮询间隔 500ms，添加位移传感器后总周期会增加。
3. **寄存器缓冲区**：确保 `MB_MASTER_TOTAL_SLAVE_NUM` 足够大（在 mbconfig.h 中定义）。
4. **线程优先级**：测试线程优先级为 26，不会干扰主要功能。

## 参考文档

- 位移传感器手册：`D:\学习资料\技术\位移传感器\003 拉绳位移传感器(RS485通信) V2.6.pdf`
- 原理图：`C:\Users\ideapad15s\Desktop\Pipe_gallery_node\Netlist_Schematic1_2026-08-31.tel`
- FreeModbus 文档：`packages/freemodbus-latest/`
