# 甲烷传感器字节顺序分析

## 问题

当前代码显示甲烷浓度为 0 ppm，需要确认字节顺序是否正确。

## 厂商参考代码

来自：`D:/学习资料/技术/GM-402B可燃气体传感器/CH4_GM-402B_CB 模块/CH4_GM-402B_CB data function_Ver1.1.c`

第138行：
```c
Value_ppm = ((uint16)buffer_in[11] << 8) + buffer_in[10];
```

**厂商定义的字节顺序：**
- `buffer_in[11]` = **高字节 (MSB)**
- `buffer_in[10]` = **低字节 (LSB)**

## 项目当前代码

文件：`applications/Apps/MethaneSensorApp.c` 第59行

```c
methane = ((rt_uint16_t)buffer_in[11] << 8) | (rt_uint16_t)buffer_in[10];
```

**当前代码字节顺序：**
- `buffer_in[11]` = **高字节**
- `buffer_in[10]` = **低字节**

✅ **与厂商代码一致！**

## 实际数据验证需要

### 之前USB-TTL捕获的数据
```
AC AC 13 AA 24 03 89 07 2A 2F 00 00 00 AA 00 00 00 00 CF
索引:                   [10][11]
                        0x2A 0x2F
```

### 按当前代码计算
```
ppm = (0x2F << 8) | 0x2A
    = 0x2F2A
    = 12074 ppm (十进制)
```

### 用户之前说的值
```
0x2A2F = 10799 ppm
```

**不匹配！差异 1275 ppm**

## 可能的原因

1. **数据帧索引理解错误**
   - 用户可能把帧头AC AC也计入了索引
   - 或者数据中有其他字段被误认为是PPM

2. **字节序理解相反**
   - 如果 `(0x2A << 8) | 0x2F = 0x2A2F = 10799`
   - 则意味着 `buffer_in[10]` 应该是高字节
   - **这与厂商代码相反！**

3. **实际捕获数据不完整或有错误**

## 下一步验证

需要用户：

1. **重新用USB-TTL捕获完整数据帧**
   - 确保包含完整19字节
   - 从第一个AC开始计数

2. **提供外部工具显示的实际PPM值**
   - 是工具自己解析出来的数值
   - 还是你手工计算的？

3. **烧录新固件后运行 `uart2_debug 10`**
   - 查看STM32实际接收到什么数据
   - 对比USB-TTL的数据

## 结论

**当前代码的字节顺序与厂商参考代码一致，应该是正确的。**

如果还是读数为0，问题可能在于：
- UART接收本身有问题（接收到错误数据）
- 帧同步有问题（AC AC帧头检测失败）
- 校验和验证失败导致数据被丢弃
