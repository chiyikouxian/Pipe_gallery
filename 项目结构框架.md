# Pipe Gallery Node 项目结构框架

## 1. 项目定位

本项目是基于 STM32H743XI 和 RT-Thread v4.0.3 的管廊环境监测节点。固件负责采集甲烷、火焰、氧气、温湿度、三相电压/电流和水流量，并通过 Ethernet、华为云 MQTT 和 LoRa 三条独立链路上报。

当前以太网链路已经完成直连铜缆和光纤收发器实板验证。外部 HTTP 上报不属于当前基线。

## 2. 软件分层

```text
applications/Apps/          传感器应用、协议编排、上报线程
        |
RT-Thread components       线程调度、设备框架、SAL、LWIP
        |
drivers/                   STM32 BSP、UART、ADC、I2C、Ethernet 驱动
        |
libraries/ + cubemx/       STM32H7 HAL、CMSIS、启动与外设配置
        |
STM32H743XI hardware       ADC/UART/GPIO/RMII 等片上外设
```

辅助组件：

- `packages/freemodbus-latest/`：Modbus RTU 主站协议栈。
- `packages/cjson/`：JSON 库；当前部分上报代码也直接使用 `rt_snprintf` 组装负载。
- `linkscripts/STM32H743XIHx/`：链接脚本和 Ethernet DMA 内存布局。
- `openspec/`：正式能力规格、活动变更和历史归档。

## 3. 应用模块

| 模块 | 职责 | 主要输出 |
|---|---|---|
| `MethaneSensorApp` | UART2 接收并校验 19 字节甲烷帧 | `g_methane_ppm`, `g_methane_lel` |
| `linesensor` / `hal_adc` | 五路火焰模拟量采集 | `g_adc_ch0/1/3/4/5`, `Flame` |
| `o2SensorApp` | ADC1 CH18 氧气采集与线性换算 | `g_o2_concentration` |
| `sht30App` | 软件 I2C1 单次测量、CRC8 校验 | `g_temperature_c`, `g_humidity_rh` |
| `freeModbusApp` | 轮询水表和三相电表 | `Flow`, `Voltage[3]`, `Current[3]` |
| `huaweiCloudApp` | ESP8266 AT 状态机和 IoTDA MQTT 上报 | 华为云属性 JSON |
| `loraApp` | ATK-LORA-01 透明传输 | 传感器文本负载 |
| `ethApp` | LWIP TCP 客户端、完整传感器 JSON | `192.168.1.100:8080` |
| `uartApp` | UART4/UART5 初始化、收发与缓冲 | ESP8266/LoRa 串口接口 |

`NodeApp` 中的 `node[]` 是早期聚合结构，部分 Modbus/旧上报代码仍会更新它；当前主要上报路径更多地直接读取各模块的全局变量。

## 4. 运行模型

[`applications/main.c`](applications/main.c) 当前启动或初始化以下线程：

| 线程 | 作用 | 周期/行为 |
|---|---|---|
| `uart4_rx` | ESP8266 响应接收 | 阻塞接收 |
| `hw_cloud` | 华为云连接与属性上报 | 状态机、失败重试 |
| `md_m_poll` | FreeModbus 主站协议轮询 | 500 ms |
| `md_m_send` | 水表、电压、电流请求 | 每类请求间隔 500 ms |
| `adc_read` | 五路火焰 ADC | 500 ms |
| `sht30_rd` | 温湿度采集 | 2000 ms |
| `o2_rd` | 氧气采集 | 2000 ms |
| `eth_tcp` | Ethernet TCP JSON 上报 | 3000 ms，失败重连 |
| `lora_tx` | LoRa 文本上报 | 周期发送 |
| `uart2_rx` | 甲烷串口帧接收 | 阻塞接收 |

## 5. 数据流

```text
UART2 Methane ───────────────┐
ADC1 Flame/O2 ───────────────┤
I2C1 SHT30 ──────────────────┼──> 模块全局变量
UART3 Modbus meters ─────────┘           |
                                        ├──> eth_tcp -> TCP JSON -> PC
                                        ├──> hw_cloud -> ESP8266/MQTT -> IoTDA
                                        └──> lora_tx -> UART5 -> LoRa
```

共享数据采用“单写线程、多读线程”的全局变量模型，没有集中消息队列、时间戳或完整快照锁。上报线程可能读取到不同采样周期的数据，但单个标量读取符合当前低频监测场景。

## 6. 外设与引脚

### UART

| 外设 | 引脚 | 用途 |
|---|---|---|
| UART1 | PA9 TX / PA10 RX | 控制台 |
| UART2 | PD5 TX / PD6 RX | 甲烷传感器 |
| UART3 | PB10 TX / PB11 RX | Modbus RTU；PA15 控制 RS485 方向 |
| UART4 | PA12 TX / PA11 RX | ESP8266 / 华为云 MQTT |
| UART5 | PC12 TX / PD2 RX | ATK-LORA-01 |

### ADC 与 I2C

| 用途 | 引脚/通道 |
|---|---|
| Flame A1 | PA0_C / ADC1_INP0 |
| Flame A2 | PA3 / ADC1_INP15 |
| Flame A3 | PA6 / ADC1_INP3 |
| Flame A4 | PA5 / ADC1_INP19 |
| Flame A5 | PB1 / ADC1_INP5 |
| O2 | PA4 / ADC1_INP18 |
| SHT30 | PB6 SCL / PB7 SDA，软件 I2C1，地址 0x44 |

### Ethernet RMII

LAN8720A 使用 PA1 REF_CLK、PA2 MDIO、PA7 CRS_DV、PC1 MDC、PC4 RXD0、PC5 RXD1、PG11 TX_EN、PG13 TXD0、PG14 TXD1。PC4/PC5 被 RMII 占用后，Flame A4 和 O2 已分别迁移到 PA5 和 PA4。

网络采用静态地址：MCU 为 `192.168.1.30/24`，PC 服务端为 `192.168.1.100:8080`。LAN8720A 通过 MDIO 软件复位，当前模块 PHY 地址为 `0x01`。

## 7. 上报覆盖范围

- Ethernet TCP JSON：包含 ADC、甲烷、电压、电流、流量、温度、湿度、氧气和火焰状态，是当前字段最完整的路径。
- 华为云 MQTT：包含 ADC、甲烷、电压、电流和流量；温湿度、氧气尚未接入当前属性负载。
- LoRa：包含 ADC、甲烷、电压、电流和流量；温湿度、氧气尚未接入当前文本负载。

三条路径互相独立，Ethernet 连接失败不会停止 MQTT、LoRa 或采集线程。

## 8. 构建与验证

- 构建入口：RT-Thread Studio 或 SCons。
- SCons 命令行构建依赖 `rtconfig.py` 中正确配置 ARM 工具链 `EXEC_PATH`。
- 主要验收仍是实板烧录、串口日志、Modbus 从站响应、云端/LoRa 接收和 Ethernet 双向网络测试。
- PC 侧 TCP 测试服务：`python tcp_server.py`。

## 9. OpenSpec 状态

- `openspec/specs/`：已落地的正式能力。
- `openspec/changes/`：仍在实现或验证的能力。
- `openspec/changes/archive/`：已完成并归档的变更。

Ethernet LAN8720A 变更已完成并归档。SHT30 和 O2 变更仍保留活动状态，直到构建、异常场景、完整板级回归以及计划内的上报集成任务得到明确处理。

项目背景以 `openspec/project.md` 为准，当前使用方式和实板结论以 `README.md` 为准，具体引脚最终以 `drivers/board.c` 和 `drivers/board.h` 为准。
