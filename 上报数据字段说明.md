# 管廊监测节点 上报 JSON 字段说明

> 面向后端接收方。本文档说明 MCU（STM32H743）通过光纤收发器 → 以太网 TCP 发送的 JSON 数据格式与每个字段的含义。

## 传输概览

| 项 | 值 | 来源 |
|----|----|----|
| 链路 | STM32H743 RMII → LAN8720A → 光纤收发器 → 电脑 | — |
| 协议 | TCP（客户端为 MCU，服务端为电脑） | [ethApp.c](applications/Apps/ethApp.c) |
| 服务端地址 | `192.168.1.100:8080` | [ethApp.h:16-17](applications/Apps/ethApp.h#L16-L17) |
| 上报周期 | 每 3000 ms 发送一帧 | [ethApp.h:23](applications/Apps/ethApp.h#L23) |
| 帧分隔 | 每帧 JSON 以 `\n`（换行）结尾，可按行分包 | [ethApp.c:123](applications/Apps/ethApp.c#L123) |
| 编码 | ASCII / UTF-8，紧凑无空格 | — |

**注意事项（后端务必知晓）：**
1. TCP 是字节流，没有天然的消息边界。请以行尾 `\n` 作为一帧的结束标志来做**粘包/拆包**处理，不要假设「一次 recv = 一帧」。
2. 所有浮点字段（voltage/current/flow/temperature/humidity/o2，以及规划中的 stress/displacement）由固件手工格式化，**固定保留 2 位小数**，且为**截断**而非四舍五入（例如 33.909 会输出 `33.90`）。
3. 单帧最大约 512 字节。当前固件实际约 260 字节；接入应力/位移后预计约 300 字节。
4. `stress`、`displacement` 为**规划字段**：当前固件尚未上报。后端解析时应按可选字段处理（缺省则忽略），不要因缺少这两个键而丢弃整帧。

## 报文示例

接入应力 / 位移传感器后的目标报文（在 `data` 末尾、`flame` 之前追加）：

```json
{"device_id":"pipe_gallery_node_01","data":{"ch0":3339,"ch1":7680,"ch3":1867,"ch4":3655,"ch5":4607,"methane_ppm":0,"methane_lel":0,"voltage_a":0.00,"voltage_b":0.00,"voltage_c":0.00,"current_a":0.00,"current_b":0.00,"current_c":0.00,"flow":0.00,"temperature":0.00,"humidity":0.00,"o2":33.90,"stress":0.00,"displacement":0.00,"flame":0}}
```

## 顶层字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `device_id` | string | 设备唯一标识，固定为 `pipe_gallery_node_01`。多节点部署时用于区分来源节点。 |
| `data` | object | 传感器数据对象，包含下述所有测量字段。 |

## `data` 内字段

### 1. 火焰传感器原始 ADC 通道（ch0 ~ ch5）

五路火焰传感器的模拟输出，经 ADC1 采集后的**原始计数值**（未做物理量换算）。

| 字段 | 类型 | 单位/量程 | 说明 |
|------|------|-----------|------|
| `ch0` | integer | 0 ~ 65535（原始 ADC 计数） | 火焰通道 A1 原始值 |
| `ch1` | integer | 0 ~ 65535 | 火焰通道 A2 原始值 |
| `ch3` | integer | 0 ~ 65535 | 火焰通道 A3 原始值 |
| `ch4` | integer | 0 ~ 65535 | 火焰通道 A4 原始值 |
| `ch5` | integer | 0 ~ 65535 | 火焰通道 A5 原始值 |

说明：
- ADC 配置为 **16 位分辨率**，因此满量程为 `65535`（见 [hal_adc.c:18](applications/Apps/hal_adc.c#L18)）。
- 字段命名沿用硬件通道号，**没有 `ch2`**（该通道未接入本采集链，属正常现象）。
- 这些是原始电压计数，数值越大代表对应通道检测到的红外/火焰信号越强；是否判定为「有火」由下文 `flame` 字段给出结论。后端一般用于趋势展示或调试，不必自行判定阈值。

### 2. 甲烷（可燃气体）

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `methane_ppm` | integer | ppm | 甲烷浓度，百万分比。经 UART2 从甲烷传感器读取。 |
| `methane_lel` | integer | %LEL | 甲烷浓度占爆炸下限的百分比（Lower Explosive Limit）。100 表示达到爆炸下限，属危险值。 |

来源：[MethaneSensorApp.h:17-18](applications/Apps/MethaneSensorApp.h#L17-L18)。两者为同一传感器给出的两种量纲，可任选或同时使用。

### 3. 三相电压 / 三相电流

来自 Modbus RTU 从站电表，A/B/C 三相。

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `voltage_a` | float(2位小数) | V（伏特） | A 相电压 |
| `voltage_b` | float(2位小数) | V | B 相电压 |
| `voltage_c` | float(2位小数) | V | C 相电压 |
| `current_a` | float(2位小数) | A（安培） | A 相电流 |
| `current_b` | float(2位小数) | A | B 相电流 |
| `current_c` | float(2位小数) | A | C 相电流 |

来源：[huaweiCloudApp.h:62-63](applications/Apps/huaweiCloudApp.h#L62-L63)。若电表未接入或 Modbus 未读到数据，值为 `0.00`。

### 4. 水流量

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `flow` | float(2位小数) | m³/h（立方米每小时） | 水流量，来自 Modbus RTU 从站水表。 |

来源：[huaweiCloudApp.h:64](applications/Apps/huaweiCloudApp.h#L64)。未接入时为 `0.00`。

### 5. 温湿度（SHT30，I2C1）

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `temperature` | float(2位小数) | °C（摄氏度） | 环境温度 |
| `humidity` | float(2位小数) | %RH | 相对湿度 |

来源：[sht30App.h:26-27](applications/Apps/sht30App.h#L26-L27)。采集周期 2000 ms。

### 6. 氧气浓度（O₂，ADC1 通道 10 / PC0）

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `o2` | float(2位小数) | %（体积百分比） | 氧气浓度。空气中正常值约 20.9%。 |

来源：[o2SensorApp.h:33](applications/Apps/o2SensorApp.h#L33)。采集周期 2000 ms。

### 7. 火焰判定结果

| 字段 | 类型 | 取值 | 说明 |
|------|------|------|------|
| `flame` | integer | `0` / `1` | 火焰检测结论：`1` = 检测到火焰，`0` = 无火焰。 |

来源：[linesensor.h:26](applications/Apps/linesensor.h#L26)。这是对上述 `ch0~ch5` 五路火焰传感器综合判定后的布尔结果，后端可直接据此告警。

### 8. 锚杆 / 锚索轴向应力

> **状态：** 接入后按与 `flow` 相同的 2 位小数浮点规则追加到 `data`。

矿用本质安全型应变式测力传感器，用于管廊支护结构（锚杆 / 锚索）轴向力在线监测。分体式结构：环形测力本体 + 外置变送器，应变片受力后由变送器转为 RS485 数字量。

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `stress` | float(2位小数) | kN（千牛） | 锚杆 / 锚索轴向应力。量程 0 ~ 400 kN，允许过载 120%。未接入或未读到时为 `0.00`。 |

传感器要点（供固件接入与后端告警阈值参考，不上报原始寄存器）：

| 项 | 值 |
|----|----|
| 原理 | 应变式测力 |
| 量程 | 0 ~ 400 kN（过载 120%） |
| 误差 | ±4% FS |
| 供电 | DC 18 V 本安电源，工作电流 ≤ 60 mA |
| 接口 | RS485 半双工，2400 / 4800 bps，最远约 2 km |
| 防护 | ExibIMb，IP54 |
| 线色 | 红 = DC18V+，白 = GND，蓝 = 485A，绿 = 485B |

后端建议：数值接近或超过支护设计预紧力时告警；`0.00` 在接入前表示未上报，接入后需与固件约定是否增加在线状态字段以区分「真实为零」和「通讯失败」。

### 9. 拉绳位移

> **状态：接入后按与 `flow` 相同的 2 位小数浮点规则追加到 `data`。

布瑞特 BRT38 系列 RS485 拉绳位移传感器，标准 Modbus-RTU。固件读取编码器值后换算为长度再上报，后端只接收物理量，不必自行解析寄存器。

| 字段 | 类型 | 单位 | 说明 |
|------|------|------|------|
| `displacement` | float(2位小数) | mm（毫米） | 拉绳伸出长度。量程依型号 0 ~ 500 ~ 10000 mm。未接入或未读到时为 `0.00`。 |

单位选用 **mm** 而非 m：现有浮点格式只保留 2 位小数，若用米则分辨率仅 1 cm，无法体现传感器 0.02 ~ 0.33 mm 量级的分辨率。

传感器要点：

| 项 | 值 |
|----|----|
| 型号 | 布瑞特 BRT38 拉绳位移传感器 |
| 协议 | Modbus-RTU，功能码 0x03 读保持寄存器 |
| 默认通信 | 9600 8N1，从站地址 1（接入 UART3 总线时需改成与水表 / 电表不冲突的站号） |
| 编码器值 | 寄存器 `0x0000~0x0001`，32 位无符号大端 |
| 圈数 / 单圈值 | `0x0002` / `0x0003`（调试用，不上报） |
| 线性精度 | ±0.1% |
| 分辨率 | 1024（10 bit）或 4096（12 bit） |
| 供电 | 5 ~ 24 V，工作电流约 100 mA |
| 线色 | 红 = 电源+，黑 = 0V，黄 = 置零（正常悬空），绿 = RS485B，白 = RS485A |

长度换算（固件侧完成，后端直接使用 `displacement`）：

```
L(mm) = (X - X0) × 轮周长(mm) / 单圈分辨率
```

- `X`：寄存器 `0x0000~0x0001` 的编码器值
- `X0`：零点基准（手册示例为拉绳未拉出时写入 1000，现场以实际置零值为准）
- 轮周长随量程变化，例如 500/1000 mm 量程为 100 mm，8~10 m 量程为 340 mm

通信示例（手册）：主站 `01 03 00 00 00 02`，从站返回 `01 03 04 00 01 76 3B` → 编码器值 `0x0001763B` = 95803。

## 字段汇总表（速查）

| 字段 | 类型 | 单位 | 分组 |
|------|------|------|------|
| `device_id` | string | — | 标识 |
| `ch0` `ch1` `ch3` `ch4` `ch5` | integer | ADC 原始计数 0~65535 | 火焰传感器原始值 |
| `methane_ppm` | integer | ppm | 甲烷 |
| `methane_lel` | integer | %LEL | 甲烷 |
| `voltage_a` `voltage_b` `voltage_c` | float | V | 三相电压 |
| `current_a` `current_b` `current_c` | float | A | 三相电流 |
| `flow` | float | m³/h | 水流量 |
| `temperature` | float | °C | 温湿度 |
| `humidity` | float | %RH | 温湿度 |
| `o2` | float | % | 氧气 |
| `stress` | float | kN | 轴向应力（规划，当前不上报） |
| `displacement` | float | mm | 拉绳位移（规划，当前不上报） |
| `flame` | integer | 0/1 | 火焰判定 |

## 后端对接建议

- **解析**：按 `\n` 分帧，逐帧做 JSON 解析；解析失败的帧应丢弃并记录，不要中断连接。未知或缺失的键（如当前尚未出现的 `stress` / `displacement`）应忽略，不要因此丢弃整帧。
- **默认值语义**：数值为 `0` 或 `0.00` 可能表示「真实为零」也可能表示「传感器/从站未接入」。当前协议不区分二者，如需区分请与固件侧约定新增在线状态字段。
- **数据落库**：建议保留 `device_id` + 服务端接收时间戳（固件目前不发送时间字段）。
- **精度**：浮点为截断 2 位小数，若后端需要更高精度需在固件侧调整格式化逻辑（[ethApp.c:28-34](applications/Apps/ethApp.c#L28-L34)）。
- **应力告警**：`stress` 单位为 kN，量程 0~400；阈值需按现场锚杆/锚索预紧力整定，不宜在后端写死 400 kN。
- **位移告警**：`displacement` 单位为 mm。管廊沉降/变形阈值按工程设计给定，固件只上报长度，不做越限判定。
</content>
</invoke>
